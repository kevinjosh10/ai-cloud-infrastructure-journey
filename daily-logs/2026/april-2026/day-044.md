# Day 044 – Strings & String Operations

**Date:** 14 April 2026  
**Duration:** ~2–2.5 Hours  
**Focus:** String Methods, Formatting, Slicing & Text Processing  

---

## 🎯 Objective

Understand string manipulation in Python using built-in methods, formatting, slicing, and multiline strings.  
Learn how to extract and process meaningful data from text in real-world scenarios.

---

## ✅ What I Completed

### 1️⃣ Learned String Methods
- Used `.upper()` and `.lower()` to change text case  
- Used `.strip()` to remove unwanted spaces  
- Used `.replace()` to modify text  
- Used `.split()` to convert strings into lists  
- Used `.join()` to combine lists into strings  

Example:
    text = "hello world"  
    text.upper() → HELLO WORLD  

    data = "apple,banana,grape"  
    data.split(",") → ['apple', 'banana', 'grape']  

    words = ["Hello", "World"]  
    " ".join(words) → Hello World  

---

### 2️⃣ Learned f-Strings (Formatted Strings)
- Used f-strings for clean and modern string formatting  
- Embedded variables directly inside strings  

Example:
    name = "Kevin"  
    age = 19  
    f"Hello {name}, you are {age} years old"  

---

### 3️⃣ Learned String Slicing
- Extracted parts of strings using indexing  
- Understood start, end, and step  

Examples:
    text = "HelloWorld"  

    text[0:5] → Hello  
    text[-3:] → rld  
    text[::2] → Hlool  

---

### 4️⃣ Learned Multiline Strings
- Used triple quotes for large text blocks  
- Useful for queries, logs, and configurations  

Example:
    query = """  
    SELECT * FROM users  
    WHERE active = 1  
    """  

---

### 5️⃣ Practiced Real-World Parsing (Hostname)

Worked with:
    prod-web-001.example.com  

Extracted:
- Environment → prod  
- Role → web  
- Domain → example.com  

Logic:
    hostname = "prod-web-001.example.com"  

    parts = hostname.split(".")  
    name_part = parts[0]  
    domain = ".".join(parts[1:])  

    name_parts = name_part.split("-")  
    environment = name_parts[0]  
    role = name_parts[1]  

---

### 6️⃣ Practiced Additional Scenario

Worked with:
    dev-db-002.internal.company.com  

Extracted:
- Environment → dev  
- Role → db  
- Domain → internal.company.com  

---

## 🧠 Key Concepts Learned

- String methods for text manipulation  
- f-strings for dynamic formatting  
- String slicing techniques  
- Multiline string usage  
- Parsing structured text like hostnames  

---

## 🔄 Mindset Shift

Moved from:

> Treating strings as simple text  

To:

> Using strings as structured data that can be parsed and processed  

---

## 💭 Reflection

Day 44 focused on one of the most important programming skills — string handling.

Built:
- Strong foundation in string manipulation  
- Ability to extract structured data from text  
- Understanding of real-world use cases in cloud and DevOps  
- Confidence in handling logs, domains, and identifiers  

---

**Status:** Day 44 Complete  
**Streak:** 44 / 1000 🚀  